// Do not need to register enum with type-graphql as it is internal only
export enum ActionTypes {
  LIST = 'LIST',
  RETRIEVE = 'RETRIEVE',
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE',
}
